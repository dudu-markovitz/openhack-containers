apiVersion: apps/v1
kind: Deployment
metadata:
  name: trips
  namespace: api
  labels:
    app: trips
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trips
  template:
    metadata:
      labels:
        app: trips
    spec:
      containers:
      - name: trips
        image: registrywlz8634.azurecr.io/api/trips:latest
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        env:
        - name: SQL_USER
          value: sqladminwLz8634
        - name: SQL_PASSWORD
          value: rX8hh5Ga6
        - name: SQL_SERVER
          value: sqlserverwlz8634.database.windows.net
        - name: SQL_DBNAME
          value: mydrivingDB
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"          
      # volumes:
      #   - name: secrets-store-inline
      #     csi:
      #       driver: secrets-store.csi.k8s.io
      #       readOnly: true
      #       volumeAttributes:
      #         secretProviderClass: "azure-kvname"
      #       nodePublishSecretRef:
      #         name: secrets-store-creds           
---
apiVersion: v1
kind: Service
metadata: 
  name: trips
spec: 
  type: ClusterIP #LoadBalancer  ClusterIP
  ports: 
  - port: 80 
    targetPort: 80
  selector: 
    app: trips

           
           
           